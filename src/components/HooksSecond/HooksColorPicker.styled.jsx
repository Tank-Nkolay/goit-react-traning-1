import styled from '@emotion/styled';

export const Box = styled.div`
  margin-bottom: 60px;
`;

export const Card = styled.div`
  display: flex;
`;

// const makeFocus = ({ index, number }) => {
//   if (index === number) {
//     return 'scale(1.1)';
//   }
// };

export const Button = styled.button`
  display: inline-block;
  margin-right: 40px;
  width: 200px;
  height: 200px;
  cursor: pointer;
`;

// transform: scale(1.2);
